version: 0.2

env:
  variables:
    DBT_TARGET: 'dev'
    DBT_DEV_HOST: '172.27.0.1'
    DBT_DEV_PORT: 5432
    DBT_DEV_USER: 'postgres'
    DBT_DEV_PASSWORD: 'lumiq121@'
    DBT_DEV_DBNAME: 'testdb'
    DBT_DEV_SCHEMA: 'public'

phases:
  install:
    runtime-versions:
      python: 3.8
    commands:
      - echo "Installing dependencies..."
      - pip install dbt-core dbt-postgres  # Adjust this line based on your specific dbt adapter
  build:
    commands:
      - echo "Building project..."
      - dbt debug  # This runs the dbt debug command to check the setup
